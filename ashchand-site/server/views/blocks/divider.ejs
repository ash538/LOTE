<section class="divider divider-<%= data.style || 'line' %> divider-<%= data.size || 'medium' %> bg-<%= data.background || 'paper' %>"
         <% if (block.anchor) { %>id="<%= block.anchor %>"<% } %> aria-hidden="true">
  <div class="shell"><% if ((data.style || 'line') !== 'space') { %><span class="divider-mark"></span><% } %></div>
</section>
